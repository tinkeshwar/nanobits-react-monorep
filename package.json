{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "version": "lerna version",
    "publish": "lerna publish from-git"
  },
  "devDependencies": {
    "lerna": "^5.3.0",
    "typescript": "^4.7.4"
  },
  "name": "nanobits-react-monorep"
}
